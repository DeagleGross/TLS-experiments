version: '3.8'

services:
  dotnet-ssl-bio:
    container_name: ssl-bio
    build:
      context: ./src-dotnet/BioSslConsole
      dockerfile: Dockerfile
    ports:
      - "5003:5003"
    volumes:
      # Mount certs directory from repository root
      - ./certs:/app/certs:ro
    environment:
      - DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=false
    restart: unless-stopped
    networks:
      - tls-experiments

networks:
  tls-experiments:
    name: tls-experiments-network
